"use strict";
function both_cases(str) {
    let lower = str.toLowerCase();
    let upper = str.toUpperCase();
    console.log(`In lower case ${lower} and in upper case ${upper}`);
}
both_cases("Hello World");
